apiVersion: v1
kind: Service
metadata:
  name: {{ include "my-ingress.fullname" . }}
  labels:
    {{- include "my-ingress.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "my-ingress.selectorLabels" . | nindent 4 }}
