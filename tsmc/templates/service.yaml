apiVersion: v1
kind: Service
metadata:
  name: {{ include "tsmc.fullname" . }}
  labels:
    {{- include "tsmc.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tsmc.selectorLabels" . | nindent 4 }}
