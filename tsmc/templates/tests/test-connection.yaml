apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tsmc.fullname" . }}-test-connection"
  labels:
    {{- include "tsmc.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "tsmc.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
